# Using

## FILES

### package.json
Update the application name at the top.

### vite.config.ts

Fix the `URL_PREFIX` as needed.

### App.tsx

Fix up `APP_NAME`.

Also the title and subtitle.

### scripts/publish_pages.sh

Update the OUTPUT_REPO_DIR variable.

## VSCODE

Be sure the editorconfig extension is installed.
